import type { ClientDefinition } from "../index.js";
export const spinClient: ClientDefinition = {
  id: "spin",
  name: "Spin",
  description: "Highlights de streaming: detecta reações, recorta, compõe moldura, legenda.",
  colors: { primary: "#7b4fd6", accent: "#e8418c" },
};
